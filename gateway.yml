app: 
  name: Spring Gateway Service
server:
  port: 8080

sku_identificator: skuId
uniq_identificator: uniqId

application:
  product:
    url: localhost:8081
    sku_endpoint: /product/sku/${sku_identificator}
    uniq_id_endpoint: /product/id/${uniq_identificator}
