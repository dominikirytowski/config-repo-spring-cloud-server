app: 
  name: Spring Gateway Service
server:
  port: 8080

application:
  product:
    url: localhost:8081
    sku_identificator: &sku skuId
    uniq_identificator: &uniq uniqId
    sku_endpoint: /product/sku/*sku
    uniq_id_endpoint: /product/id/*uniq
